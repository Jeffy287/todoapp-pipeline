apiVersion: v1
kind: Service
metadata:
  name: todoapp-service
spec:
  type: NodePort
  selector:
    app: todoapp
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080  # This is the port exposed by Minikube
